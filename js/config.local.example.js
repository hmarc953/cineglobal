/**
 * Copiar este archivo como `config.local.js` y completar la clave personal.
 * `config.local.js` está ignorado por Git y no debe versionarse.
 */
window.CINEGLOBAL_CONFIG = {
  TMDB_API_KEY: ''
};
